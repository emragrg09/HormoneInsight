# Environment configuration for Hormone Insight
#
# The frontend calls the FastAPI backend described in FRONTEND_HANDOFF.md.
# Point this at the running API base URL (no trailing slash).

VITE_API_BASE_URL=http://127.0.0.1:8000
